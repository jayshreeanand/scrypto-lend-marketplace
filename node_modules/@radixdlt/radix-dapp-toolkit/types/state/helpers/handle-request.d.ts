import { Logger } from 'tslog';
import { WalletClient } from '../../wallet/wallet-client';
import { DataRequestValue, State } from '../../_types';
export type HandleRequestOutput = ReturnType<typeof handleRequest>;
export declare const handleRequest: (dataRequest: DataRequestValue, { state, logger, walletClient, }: {
    state?: State | undefined;
    logger?: Logger<unknown> | undefined;
    walletClient: WalletClient;
}) => import("neverthrow").ResultAsync<{
    resolvedBy: string;
    persist: boolean;
    data: {
        accounts: {
            address: string;
            label: string;
            appearanceId: number;
        }[];
        persona: {
            label: string;
            identityAddress: string;
        } | undefined;
    };
}, import("@radixdlt/wallet-sdk").SdkError>;
